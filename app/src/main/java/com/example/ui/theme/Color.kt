package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Dark Theme Palette (2026 Deep Slate / Sky Blue) — updated from Emerald to Sky Blue
val DarkBg = Color(0xFF090D16)          // Deep Midnight Slate
val DarkSurface = Color(0xFF131B2E)     // Elevated Navy Glass Surface
val DarkSurfaceVariant = Color(0xFF1E293B) // Slate 800 Surface Variant
val DarkBorder = Color(0xFF334155)      // Slate 700 Border

val AccentBlueDark = Color(0xFF0EA5E9)       // Vibrant Sky Blue (was AccentGreenDark)
val AccentBlueDarkContainer = Color(0xFF0F2233) // (was AccentGreenDarkContainer)

val DarkTextPrimary = Color(0xFFF8FAFC)
val DarkTextSecondary = Color(0xFF94A3B8)
val DarkTextMuted = Color(0xFF64748B)

// Light Theme Palette (2026 Soft Sky / Ocean Blue) — updated from Mint/Forest Green
val LightBg = Color(0xFFF4F7FA)         // Dynamic Soft Canvas Background
val LightSurface = Color(0xFFFFFFFF)    // Pure White Surface
val LightSurfaceVariant = Color(0xFFE7EEF2) // Soft Blue Tint Surface Variant
val LightBorder = Color(0x14000000)     // Subtle 0.08 alpha border

val AccentBluePrimary = Color(0xFF0284C7)   // (was AccentGreenPrimary)
val AccentBlueGradientStart = Color(0xFF0EA5E9)
val AccentBlueGradientEnd = Color(0xFF0369A1)

val LightTextPrimary = Color(0xFF1A1A1A)
val LightTextSecondary = Color(0xFF757575)
val LightTextMuted = Color(0xFF9E9E9E)

// Status Colors (kept semantic — unrelated to brand accent)
val StatusPendingBg = Color(0xFF451A03)
val StatusPendingText = Color(0xFFFBBF24) // Yellow/Amber

val StatusApprovedBg = Color(0xFF172554)
val StatusApprovedText = Color(0xFF60A5FA) // Blue

val StatusCompletedBg = Color(0xFF052E16)
val StatusCompletedText = Color(0xFF4ADE80) // Green (growth/success stays green)

val StatusCancelledBg = Color(0xFF450A0A)
val StatusCancelledText = Color(0xFFF87171) // Red
